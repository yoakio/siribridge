services:
  siribridge:
    build: .
    container_name: siribridge
    network_mode: host  # 使用 host 模式以便直接通过 Tailscale IP 访问网关
    restart: always
    env_file: .env
    volumes:
      - ./siri_bridge.log:/app/siri_bridge.log
